# S3 ORBIT

Dock to the arm. Too hard a contact is a fail.

Play it in the browser: https://macncrash.github.io/s3games/play/s3orbit/

## Design

Soft dock or no dock. A hard contact on the arm fails even if you touched it.

The browser build is one file. The link above is that file, not a capture of a desktop session.
