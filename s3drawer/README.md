# S3 DRAWER

Close the shop. The drawer matches the tape.

Play it in the browser: https://macncrash.github.io/s3games/play/s3drawer/

## Design

The drawer has to match the tape, then the shop closes. A till that is close is still open.

The browser build is one file. The link above is that file, not a capture of a desktop session.
