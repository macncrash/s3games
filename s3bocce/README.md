# S3 BOCCE

First to seven. Closest to the little ball.

Play it in the browser: https://macncrash.github.io/s3games/play/s3bocce/

## Design

First to seven, closest to the little ball. A cluster that is near but second does not take the end.

The browser build is one file. The link above is that file, not a capture of a desktop session.
