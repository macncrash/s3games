# S3 CLOCK

Three hands. The hour has to chime.

Play it in the browser: https://macncrash.github.io/s3games/play/s3clock/

## Design

Three hands, and the rope only counts on a true hour. Hauling early does not chime.

The browser build is one file. The link above is that file, not a capture of a desktop session.
