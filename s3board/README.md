# S3 BOARD

Connect the calls before they drop.

Play it in the browser: https://macncrash.github.io/s3games/play/s3board/

## Design

Connect the calls before they drop. A line that rings and then falls does not count.

The browser build is one file. The link above is that file, not a capture of a desktop session.
