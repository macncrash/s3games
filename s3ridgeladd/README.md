# S3 RIDGELADD

One ridge. Reach the far ladder. Then it is done.

Play it in the browser: https://macncrash.github.io/s3games/play/s3ridgeladd/

## Design

The far ladder is the job. Stopping short of it is a loss.

The browser build is one file. The link above is that file, not a capture of a desktop session.
