# S3 RIDGE

They come over the crest. Hold the path.

Play it in the browser: https://macncrash.github.io/s3games/play/s3ridge/

## Design

They come over the crest. Holding the path is the job. Chasing them off the path loses the ridge.

The browser build is one file. The link above is that file, not a capture of a desktop session.
