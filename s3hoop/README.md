# S3 HOOP

First to twenty-one. The rim is the judge.

Play it in the browser: https://macncrash.github.io/s3games/play/s3hoop/

## Design

First to 21, and the rim is the judge. A ball that looks in and kicks out is out.

The browser build is one file. The link above is that file, not a capture of a desktop session.
