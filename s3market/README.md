# S3 MARKET

One stall. Right change. The line has to move.

Play it in the browser: https://macncrash.github.io/s3games/play/s3market/

## Design

Right change, and the line has to move. A correct till with a stalled queue is not a finished stall.

The browser build is one file. The link above is that file, not a capture of a desktop session.
