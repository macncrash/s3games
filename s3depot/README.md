# S3 DEPOT

Clear the yard.

Play it in the browser: https://macncrash.github.io/s3games/play/s3depot/

## Design

The yard has to be clear. A load left on the dock is an unfinished shift.

The browser build is one file. The link above is that file, not a capture of a desktop session.
