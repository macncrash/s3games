# S3 BARGE

Enter the lock, rise, leave without scraping.

Play it in the browser: https://macncrash.github.io/s3games/play/s3barge/

## Design

Enter, rise, leave. A scrape on the gate fails the lock even if you made the height.

The browser build is one file. The link above is that file, not a capture of a desktop session.
