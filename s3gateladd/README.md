# S3 GATELADD

You have the gate. The job is to reach the far ladder. Anything else is a loss.

Play it in the browser: https://macncrash.github.io/s3games/play/s3gateladd/

## Design

The far ladder is the job. Stopping short of it is a loss.

The browser build is one file. The link above is that file, not a capture of a desktop session.
