# S3 EIGHT

One rack. Call the pocket.

Play it in the browser: https://macncrash.github.io/s3games/play/s3eight/

## Design

Call the pocket before the 8. The rack is cleared only when that called ball goes where you said.

The browser build is one file. The link above is that file, not a capture of a desktop session.
