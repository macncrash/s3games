# S3 COMBINE

One pass. The header stays full.

Play it in the browser: https://macncrash.github.io/s3games/play/s3combine/

## Design

One pass, and the header has to stay full. A fast pass that spills the header is a failed field.

The browser build is one file. The link above is that file, not a capture of a desktop session.
