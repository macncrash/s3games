# S3 TABLE

First to seven. The puck has to cross.

Play it in the browser: https://macncrash.github.io/s3games/play/s3table/

## Design

First to seven, and the puck has to cross. A puck that dies on the line is not a point.

The browser build is one file. The link above is that file, not a capture of a desktop session.
