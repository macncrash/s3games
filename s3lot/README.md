# S3 LOT

Three targets, then the gate, before the clock.

Play it in the browser: https://macncrash.github.io/s3games/play/s3lot/

## Design

Three targets, then the gate, before the clock. Clearing the lot and missing the gate is not out.

The browser build is one file. The link above is that file, not a capture of a desktop session.
