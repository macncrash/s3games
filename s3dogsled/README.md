# S3 DOGSLED

The checkpoint. The team turns with you.

Play it in the browser: https://macncrash.github.io/s3games/play/s3dogsled/

## Design

The checkpoint is the win, and the team turns with you. A spill ends the run even if the lantern is lit.

The browser build is one file. The link above is that file, not a capture of a desktop session.
