# S3 SUB

A dark channel. Don't scrape the bottom.

Play it in the browser: https://macncrash.github.io/s3games/play/s3sub/

## Design

The channel is dark and the bottom is the fail. Depth is a limit, not a resource you spend.

The browser build is one file. The link above is that file, not a capture of a desktop session.
