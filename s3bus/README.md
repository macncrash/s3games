# S3 BUS

Six stops. The doors open only in the box.

Play it in the browser: https://macncrash.github.io/s3games/play/s3bus/

## Design

The doors open only in the box. A stop where you are close is a missed stop.

The browser build is one file. The link above is that file, not a capture of a desktop session.
