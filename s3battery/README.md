# S3 BATTERY

The road below. Stop the column.

Play it in the browser: https://macncrash.github.io/s3games/play/s3battery/

## Design

The column is the target and the gate is the fail. A gun still firing after a truck has passed has already lost.

The browser build is one file. The link above is that file, not a capture of a desktop session.
