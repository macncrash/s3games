# S3 LOCK

One lock. Don't hit the gates.

Play it in the browser: https://macncrash.github.io/s3games/play/s3lock/

## Design

One lock, and the gates are the fail. Making the height while scraping a leaf is not a clear pass.

The browser build is one file. The link above is that file, not a capture of a desktop session.
