# S3 BUNKER

One room, one door. Hold it.

Play it in the browser: https://macncrash.github.io/s3games/play/s3bunker/

## Design

One room, one door. The room holding is the win, not the number you dropped outside it.

The browser build is one file. The link above is that file, not a capture of a desktop session.
