# S3 FERRY

Dock in the slip before the tide clock.

Play it in the browser: https://macncrash.github.io/s3games/play/s3ferry/

## Design

The tide clock is the deadline. Pretty handling after the tide has turned does not dock the boat.

The browser build is one file. The link above is that file, not a capture of a desktop session.
