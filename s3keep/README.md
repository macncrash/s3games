# S3 KEEP

The door, for three minutes.

Play it in the browser: https://macncrash.github.io/s3games/play/s3keep/

## Design

Three minutes on the door. The score is that the door held, not how many you dropped.

The browser build is one file. The link above is that file, not a capture of a desktop session.
