# S3 DUEL

Three paces. Fire early and you lose.

Play it in the browser: https://macncrash.github.io/s3games/play/s3duel/

## Design

Three paces, then the draw. Firing early is a loss even if the shot would have hit.

The browser build is one file. The link above is that file, not a capture of a desktop session.
