# S3 DRIFT

One pass. Score the slide. The wall ends it.

Play it in the browser: https://macncrash.github.io/s3games/play/s3drift/

## Design

One pass. The slide is the score and the wall is the end. A second run is not offered.

The browser build is one file. The link above is that file, not a capture of a desktop session.
