# S3 THERM

One envelope. The mark, not the trees.

Play it in the browser: https://macncrash.github.io/s3games/play/s3therm/

## Design

One envelope. The mark is the landing and the trees are not. Time in the air is not the score.

The browser build is one file. The link above is that file, not a capture of a desktop session.
