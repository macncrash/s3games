# S3 MAZE

A hedge from the path. The exit is the only win.

Play it in the browser: https://macncrash.github.io/s3games/play/s3maze/

## Design

The exit is the only win. Mapping the hedge without leaving it does not count.

The browser build is one file. The link above is that file, not a capture of a desktop session.
