# S3 STRIKER

Three swings. Ring the bell.

Play it in the browser: https://macncrash.github.io/s3games/play/s3striker/

## Design

Three swings to ring the bell. A swing that looks close and does not ring is a miss, not a point.

The browser build is one file. The link above is that file, not a capture of a desktop session.
