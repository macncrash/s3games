# S3 MEMORY

Match the table before the clock.

Play it in the browser: https://macncrash.github.io/s3games/play/s3memory/

## Design

Match the table before the clock. A pair left over when time dies is not a cleared table.

The browser build is one file. The link above is that file, not a capture of a desktop session.
