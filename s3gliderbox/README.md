# S3 GLIDERBOX

The glider has one job: stop inside the box.

Play it in the browser: https://macncrash.github.io/s3games/play/s3gliderbox/

## Design

Stop inside the box. Close to the box is still outside.

The browser build is one file. The link above is that file, not a capture of a desktop session.
