# S3 PUTTSEVEN

Play putt until first to seven. Leave when that is true.

Play it in the browser: https://macncrash.github.io/s3games/play/s3puttseven/

## Design

First to seven holes, then leave. A six that looks close is still short.

The browser build is one file. The link above is that file, not a capture of a desktop session.
