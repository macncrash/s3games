# S3 YARDCOLU

You have the yard. The job is to stop the column on the road. Anything else is a loss.

Play it in the browser: https://macncrash.github.io/s3games/play/s3yardcolu/

## Design

The column has to stop on the road. A truck that gets through is a loss.

The browser build is one file. The link above is that file, not a capture of a desktop session.
