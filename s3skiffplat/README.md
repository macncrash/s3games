# S3 SKIFFPLAT

The skiff has one job: stop level with the platform.

Play it in the browser: https://macncrash.github.io/s3games/play/s3skiffplat/

## Design

Stop level with the platform. Close is not level.

The browser build is one file. The link above is that file, not a capture of a desktop session.
