# S3 SHUFFLE

First to fifteen. The disk has to stop in the score.

Play it in the browser: https://macncrash.github.io/s3games/play/s3shuffle/

## Design

First to fifteen, and the disk has to stop in the score. A disk that slides through does not count.

The browser build is one file. The link above is that file, not a capture of a desktop session.
